# Pin versions so the local build and the CI build agree.
# Bump these only when intentionally testing a newer MkDocs Material.
mkdocs==1.6.1
mkdocs-material==9.5.49
mkdocs-material-extensions==1.3.1
mike==2.1.3        # optional: site versioning (v1.0, v1.1, v1.2...)
pymdown-extensions==10.12
pygments==2.18.0
